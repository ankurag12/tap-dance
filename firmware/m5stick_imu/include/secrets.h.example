// Copy this file to secrets.h (same directory) and fill in real values.
// secrets.h is gitignored — do NOT commit Wi-Fi credentials.
#pragma once

#define WIFI_SSID  "your-wifi-ssid"
#define WIFI_PASS  "your-wifi-password"

// The Jetson's LAN IP where the micro-ROS agent runs (find via `hostname -I`),
// and the agent's UDP port (matches the `udp4 --port` you start the agent with).
#define AGENT_IP   "192.168.1.42"
#define AGENT_PORT 8888
